language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-videojs-plugin' ]; then cd .. && eval "mv $currentfolder generator-videojs-plugin" && cd generator-videojs-plugin; fi

